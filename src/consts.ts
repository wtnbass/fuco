export const contextTypePrefix = "fuco:context:";

export const contextType =
  contextTypePrefix + Math.random().toString(36).slice(2);

export const errorTypePrefix = "fuco:error:";

export const errorType = errorTypePrefix + Math.random().toString(36).slice(2);
